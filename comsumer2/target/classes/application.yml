server:
  port: 8002
spring:
  application:
    name: comsumer2
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka